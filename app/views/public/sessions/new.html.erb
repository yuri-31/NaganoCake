<div class="conatiner px-sm-0">
  <div class="row">
    <div class="col-8 mx-auto">
      <h4 class="bg-light d-inline-block px-3">ログイン</h4>
      <div class="col-10">
        <p class="bg-light d-inline-block px-3">会員の方はこちらからログイン</p>
      </div>
      <div class="text-center col-10 mx-auto">
      <%= form_for(resource, as: resource_name, url: session_path(resource_name)) do |f| %>
        <table class="table table-borderless table-sm">
          <tbody>
            <tr class="field">
              <th class="text-left"><%= f.label :email, "メールアドレス" %></th>
              <td><%= f.email_field :email, autofocus: true, autocomplete: "email", placeholder: "sample@example.com", class: 'rounded col-12' %></td>
            </tr>
            <tr class="field">
              <th class="text-left"><%= f.label :password, "パスワード" %></th>
              <td><%= f.password_field :password, autocomplete: "current-password", class: 'rounded col-12' %></td>
            </tr>
            <tr class="actions">
              <th colspan="1"></th>
              <td><%= f.submit "ログイン", class: 'text-white btn btn-primary btn-block col-6 float-left' %></td>
            </tr>
          </tbody>
        </table>
      <% end %>
      </div>
      <div class="col-10">
        <p class="bg-light d-inline-block px-3">会員登録がお済みでない方</p>
        <p class="text-center"><%= render "public/shared/links" %>から新規登録を行ってください。</p>
      </div>
    </div>
  </div>
</div>


        <!--% if devise_mapping.rememberable? %>-->
        <!--  <div class="field">-->
        <!--    %= f.check_box :remember_me %>-->
        <!--    %= f.label :remember_me %>-->
        <!--  </div>-->
        <!--% end %>-->